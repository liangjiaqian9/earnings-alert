name: Stock Monitor

on:
  schedule:
    # 美东时间早上9点（开盘前）和下午4点（收盘后）运行
    # UTC时间 13:00 和 21:00
    - cron: '0 13 * * 1-5'
    - cron: '0 21 * * 1-5'
  workflow_dispatch:  # 允许手动触发

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install yfinance requests pandas
    
    - name: Run stock monitor
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: python monitor.py
```

---

## 第六步：测试运行

在 GitHub 仓库页面：
1. 点击 **Actions** 标签
2. 选择 **Stock Monitor**
3. 点击 **Run workflow** 手动触发
4. 查看运行日志，检查 Telegram 是否收到消息

---

## 效果示例

你的 Telegram 会收到这样的消息：
```
📊 AAPL 财报即将发布！
📅 日期：2025-02-01
⏰ 还有 3 天

📰 NVDA 最新新闻：
- NVIDIA宣布新一代GPU...
- 分析师上调NVDA目标价...
